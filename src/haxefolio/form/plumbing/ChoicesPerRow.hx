package haxefolio.form.plumbing;

/*
    A two-value switch on `ResponsivityController.isCollapsed`: `expanded` when it is `false`,
    `collapsed` when it is `true`.
*/
typedef ChoicesPerRow = {
    expanded:Int,
    collapsed:Int
}
